// Placeholder for shared types

export interface User {
  id: string;
  email: string;
  name: string;
}

export interface Track {
  id: string;
  title: string;
  artist: string;
  album: string;
  duration: number;
}